import 'package:flutter/material.dart';

class LoginKuPage extends StatelessWidget {
  const LoginKuPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.lightGreen,
      body: Center(
        child: Text("KU-ALL Login Page")
      ),
    );
  }
}